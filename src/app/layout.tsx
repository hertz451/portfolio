import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Navigation } from "@/components/ui/navigation";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Portfolio | Ömer Faruk Kazar",
  description: "Software Engineer specializing in iOS Engineering. Passionate about crafting elegant, performant mobile applications with clean code and exceptional user experiences",
  keywords: ["software engineer", "swift developer", "iOS developer", "swift programming", "iOS development", "UIKit", "SwiftUI", "mobile development", "clean architecture"],
  authors: [{ name: "Ömer Faruk Kazar" }],
}

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Navigation />
        <main>{children}</main>
      </body>
    </html>
  );
}
